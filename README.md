# Landscape Generator

This is a simple Svelte+d3 project to explore both svelte and histing d3 content in git-hub pages. On loading the page, a random data set is generated that is then used to create a scene. The swap button toggles between and city scape and a forest, both of which are drawn using d3 based on the random array.


